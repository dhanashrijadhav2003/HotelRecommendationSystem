<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Update City</title>
  <link rel="stylesheet" href="/Newcity.css" />
  <link href="https://fonts.googleapis.com/css2?family=Segoe+UI&display=swap" rel="stylesheet" />
</head>
<body>

  <div class="box">
    <form action="/finalCityUpdate" method="POST">

      <h2 style="margin-bottom: 20px; color: black; text-align:center">Update City</h2>

      <!-- Hidden or read-only city ID -->
      <div class="form-group">
        <input type="text" name="city_id" value="<%= city.city_id %>" readonly />
      </div>

      <div class="form-group">
        <input type="text" name="city_name" value="<%= city.city_name %>" placeholder="Enter City Name" required />
      </div>

      <div class="form-group">
        <input type="text" name="pincode" value="<%= city.pincode %>" placeholder="Enter Pincode" required />
      </div>

      <div class="form-group">
        <input type="submit" value="✅ Update City" />
      </div>

      <% if (typeof msg !== 'undefined') { %>
        <span><%= msg %></span>
      <% } %>

    </form>
  </div>

</body>
</html>
